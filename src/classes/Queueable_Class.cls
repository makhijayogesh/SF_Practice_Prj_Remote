public class Queueable_Class implements Queueable{
    
        Public void Execute(QueueableContext con)
        {
            Account a = new Account(Name='Sample Account1');
            insert a;
           
        }

}